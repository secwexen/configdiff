# configdiff
A simple CLI tool to compare two configuration files and highlight the differences. Useful for developers, DevOps engineers, and security teams who need to track changes between environments (dev, staging, prod).
